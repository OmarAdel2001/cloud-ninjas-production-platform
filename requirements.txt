# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Database (adjust based on your stack)
sqlalchemy==2.0.36
psycopg2-binary==2.9.10          # if using PostgreSQL
# OR
pymysql==1.1.1                   # if using MySQL
# OR
pymongo==4.10.1                  # if using MongoDB

# Async support (if needed)
httpx==0.27.2

# Validation & serialization
pydantic==2.10.3

# Logging, config, utilities
python-dotenv==1.0.1
loguru==0.7.2

# Testing & CI support (optional but useful in dev)
pytest==8.3.3
requests==2.32.3
coverage==7.6.1                  # for coverage.xml (used by SonarCloud)

# Optional: if you run smoke tests in CI that use Python
